# Batch-Pipeline-CSV-Kafka-Spark
A pipeline that extracts data from a CSV using Kafka, applies aggregation, flag creation, column extraction transformations, and writes as CSV to local storage using Spark.
